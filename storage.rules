rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Public read access for portfolio images/pdfs.
      allow read: if true;
      // Only authenticated admin can upload/delete files.
      allow write: if request.auth != null;
    }
  }
}
