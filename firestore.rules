rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Public read access for the portfolio visitors.
      allow read: if true;
      // Only authenticated admin can modify the database.
      allow write: if request.auth != null;
    }
  }
}
